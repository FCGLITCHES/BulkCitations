/**
 * @deprecated Use `pnpm --filter server diagnostics:fixed-batch-smoke` instead.
 */
import '../../src/diagnostics/fixedBatchQualityReport.js';

console.error('Use: pnpm --filter server diagnostics:fixed-batch-smoke');
process.exit(1);
