# Plan Later

> **Status (2026-06-25):** ML/BIO evidence and engine-v2 backlog. Despite the similar name, this file is **not** a duplicate of `later-plan.md` (which is the documentation-structure / cross-domain plan hub) — this one tracks ML/BIO promotion-evidence and GROBID-baseline engine-v2 work. Progress to date: the active BIO corpus is `ml-service/datasets/citation-bio/processed/real_train_v1.jsonl`, the approved-truth certification model is implemented (`server/src/training/truthCertification.ts`), and the first evidence priorities (type-ML shadow eval, health/hotspot reconciliation, duplicate precision audit, separated throughput lanes, BIO-primary promotion gates) are marked completed below. The remaining items are still pending roadmap. See `engine/system-assessment.md` and `ml-system/ml-bio-engine-priority-plan-2026-04-30.md` for live status.

- 2026-04-30 Simplified ML/BIO admin plan: present the work as one Parser Release Readiness flow with four admin cards (Parsing Quality, Real Site Speed, Training Data Health, Safe Rollout), backed by a single evidence bundle and advanced technical drawer; implement as Evidence Bundle, Real Website Speed, Training Data Health, Candidate Vs Current Safety, and Release Readiness UI without changing live parser behavior.
- 2026-04-30 ML/BIO engine priority plan: execute two parallel workstreams from `docs/ml-system/ml-bio-engine-priority-plan-2026-04-30.md`: throughput truth first for direct/backend/queued/browser lanes, and BIO promotion truth first for export audit, BIO evaluation metrics, admin gates, and Phase 4 attribution-only diagnostics before any runtime behavior change.
- Implement the ML/BIO engine evidence plan in this order: effective policy/lane truth, non-regression gate contract, per-reference field attribution, BIO entity/field metrics, type ML shadow-vs-heuristic evaluation, health/hotspot reconciliation, duplicate precision audit, separated throughput lanes, then BIO-primary candidates only after proof.
- Completed evidence priorities after the first four report tickets: type ML shadow-vs-heuristic evaluation, health/hotspot reconciliation, duplicate precision audit with cause attribution, separated throughput lanes, and BIO-primary promotion gates.
- Next evidence priorities: measure the normal backend convert route and queued job lanes directly, then add the real site converter browser lane alongside the admin-diagnostics browser lane.
- Add an effective-policy block to diagnostics and parse artifacts showing requested/effective parse profile, runtime profile, phase ML flags, override reasons, route type, model/data lineage, certified rows loaded, and quarantined/draft rows skipped.
- Define the evidence gate contract before behavior changes: split/field/rendered/semantic hashes, ready/review/action transition matrix, zero critical field loss, zero malformed BIO accepted, no unexpected provider usage, and explicit baseline-update mode only.
- Extend ML/BIO evidence output with per-reference, per-field paired diffs for heuristic, raw ML, raw BIO, guarded BIO, hybrid final, and gold values, including accepted source, rejected reason, readiness delta, and rendered-text delta.
- Add BIO quality metrics beyond token accuracy: entity/field/span F1, per-field coverage, malformed/overlap diagnostics, punctuation false positives, critical-field recall, accepted patch precision, rejected-would-regress counts, and browser workload parity.
- Keep type ML as shadow/guarded until it beats heuristic/structural routing on gold and hard workloads without health, schema, rendered-output, or readiness regressions.
- Reconcile action-needed counts with visible hotspots by requiring every action-needed reference to have a visible reason or be counted as a hidden/internal reason.
- Audit duplicate detection separately by pair and cluster precision/recall with cause attribution for normalized hash, DOI, canonical work key, and MinHash/LSH before changing thresholds or disabling rules.
- Expand citation BIO label set from v1 high-priority fields to v2 (`bookTitle`, `websiteTitle`, `conferenceName`, `edition`, `accessDate`, `isbn`, `issn`, `publicationDate`) after coverage thresholds are met.
- Add deterministic dataset splitting script with dedupe-aware stratification and a frozen challenge set (`citation-bio-challenge-v1`).
- Add Hugging Face `DatasetDict` builder with typed features (`ClassLabel`) generated from span-derived BIO rows.
- Add disagreement and coverage report generation in `ml-service/datasets/citation-bio/reports/` tied to annotation guide and label set versions.
- Add active-learning sampling loop for low-confidence and resolver-disagreement cases after the v1 baseline model run.
- Wire Phase 4 to consume native BIO entity spans from the ONNX extractor instead of rebuilding spans from field text so Phase 10 can score real sequence errors (`overlapping_spans`, `unclosed_bio_sequence`, `missing_required_ml_span`).
- Add a first-class BIO extraction contract in the ML service and engine metadata (`entities`, label sequence diagnostics, span-grounding provenance) so BIO tagging is a used runtime signal, not just a training artifact.
- Persist learning-queue and approved-truth promotions into BIO-ready supervision rows with stable raw-text hashes plus explicit field/span corrections, then feed those rows back into `ml-service/datasets/citation-bio/processed/` for retraining.
- Add a promotion gate requiring Phase 4 shadow-history win/loss and end-to-end engine benchmarks before a BIO bundle replaces the active extraction model.
- Design a GROBID-baseline engine v2 plan that keeps the current benchmark harness, authority cache, style/type heuristics, rendering, and admin curation surfaces, but collapses the runtime phases into fewer explicit stages: ingest/split, style/type routing, candidate extraction with BIO plus deterministic resolvers, normalization/validation, render, and feedback.
- Extend the engine v2 plan with a GROBID-compatible PDF/layout lane: layout-token extraction, bibliography-zone detection, reference-boundary segmentation, token-level reference parsing, entity normalization, and citation-context linking, each with separate benchmark gates.
- Add benchmark parity tracks for GROBID-style intrinsic reference parsing, PDF bibliography extraction plus parsing, and full PDF citation-context linking before treating the engine as a GROBID replacement.
