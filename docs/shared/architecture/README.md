# Architecture

This section describes the product as a whole rather than any single subsystem.

## Contents

- `system-topology.md`
  - Canonical current-state map: deployable components (Render backend, Vercel frontend, FastAPI ML service), repository layout, and how requests move through the system.
