# Engine V2 Mirrors

Generated mirrors for older consumers belong here when they are not maintained
in the historical `ml-service` paths.

Mirrors must have a sibling `.manifest.json` file containing:

- `source`
- `mirror`
- `dataset_version`
- `created_at`
- `sha256`
