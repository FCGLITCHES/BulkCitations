# Engine V1 Dataset Mirrors

This folder documents v1 compatibility mirrors only. Active gold datasets move to
`datasets/engine-v2/gold` when engine v2 is present.

Do not edit mirror files manually. Mirrors must be generated from the v2
source-of-truth datasets or resolved through the dataset resolver fallback layer.
