# V1 Mirrors

Compatibility exports for legacy v1 scripts belong here when they are not kept
in the historical `ml-service` paths.

Current legacy paths remain supported until every script is migrated:

- `ml-service/training/style_gold.jsonl`
- `ml-service/training/gold-datasets/`
- `ml-service/datasets/citation-bio/`
